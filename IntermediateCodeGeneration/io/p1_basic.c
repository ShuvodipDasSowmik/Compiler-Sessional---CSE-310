int a, b;

int main() {
    int x, y;

    a = 10;
    b = 20;
    x = 100;
    y = 200;

    println(a, b, x, y);
    println(a * 2 + b, x + y - 10, 3 + 3 * 7);
    println(a == b, a && b, a++, -a);

    return 0;
}